<script webc:type="js" webc:is="template">
  let siteTitle;

  if (site.title === title) {
    siteTitle = title;
  } else {
    siteTitle = `${title} | ${site.title}`;
  }

  `<title>${siteTitle}</title>`;
</script>
